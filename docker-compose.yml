services:
  web:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./media:/app/media
      - ./gee-key.json:/app/gee-key.json:ro
      - modis_data:/data/modis

volumes:
  modis_data:
