Django==5.1.15
psycopg[binary]==3.3.3
python-dotenv>=1.0.0
Pillow>=10.0.0
djangorestframework>=3.15.0
drf-spectacular>=0.27.0
tzdata>=2025.1
requests>=2.31.0
feedparser>=6.0.0
django-redis>=5.4.0
django-ratelimit>=4.1.0
openpyxl>=3.1.0
xlrd>=2.0.0
earthengine-api>=1.4.0
numpy>=2.1
scipy>=1.13
pandas>=2.2
rasterio>=1.3
# Pure-python shapefile writer: DJI Agras prescription map export (VRA).
pyshp>=2.3
# S3/MinIO client: presigned multipart upload + object ops for raster layers
# (my_fields ГИС). GDAL /vsis3/ reads COG directly from S3 by range requests.
boto3>=1.34
# Required by `manage.py test --parallel`: serialises exception tracebacks
# across worker processes (without it, failures raise "can't pickle traceback").
tblib>=3.0
# Error tracking -> self-hosted GlitchTip (Sentry-compatible). No-op unless
# SENTRY_DSN is set in the environment (see enb_django/settings.py).
sentry-sdk[django]>=2.0

# GDAL / GEOS are installed at OS level.
# On Ubuntu: apt-get install -y gdal-bin libgdal-dev libgeos-dev
# Python binding version must match installed GDAL:
# gdal==<version matching gdal-config --version>
