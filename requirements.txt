Django==5.1.15
psycopg[binary]==3.3.3
python-dotenv>=1.0.0
Pillow>=10.0.0
djangorestframework>=3.15.0
drf-spectacular>=0.27.0
tzdata>=2025.1
requests>=2.31.0
feedparser>=6.0.0
django-redis>=5.4.0
openpyxl>=3.1.0

# numpy is NOT installed — server CPU lacks X86_V2 for numpy 2.x,
# and numpy 1.x doesn't support Python 3.13.
# Django GIS works fine without it; we use CDSE Statistical API.

# GDAL / GEOS are installed at OS level.
# On Ubuntu: apt-get install -y gdal-bin libgdal-dev libgeos-dev
# Python binding version must match installed GDAL:
# gdal==<version matching gdal-config --version>
